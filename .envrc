# After `direnv allow`, load the same environment as `nix develop` whenever a
# shell enters this directory. Keeping this file to one command ensures that
# direnv adds no setup beyond the development shell defined in flake.nix.
use flake
